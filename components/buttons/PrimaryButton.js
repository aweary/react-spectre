import React, { Component } from 'react';
import DefaultButton from './DefaultButton';

const PrimaryButton = (props) => (
  <DefaultButton {...props} type='btn-primary' />
)

export default PrimaryButton
